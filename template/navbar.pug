link
//div(class="logo")
    //a(href="/home"): img(alt="PileOfGames Logo" src="/public/img/logo.png" width="400px")


ul(class="navbar")

    //Startseite
    li: a(href="/home") Startseite

    //Spiele DropDwn
    //li(class="dropdown")
        a(href="javascript:void(0)", class="dropbtn") Spiele
        div(class="dropdown-content")
            a(class="dropdown" href="/games") Suche

    //Plattformen DropDwn
    //li(class="dropdown")
        a(href="javascript:void(0)" class="dropbtn") Plattformen
        div(class="dropdown-content")
            a(class="dropdown" href="/platforms") Suche

    //Hilfe DropDwn
    //li(class="dropdown")
        a(href="javascript:void(0)" class="dropbtn") Hilfe
        div(class="dropdown-content")
            a(class="dropdown" href="/about") Über
            a(class="dropdown" href="/faq") FAQ
            a(class="dropdown" href="/impress") Impressum

    //Suche
    li(style="float: right; alignment: center; margin-right: 8px")
        form(class="searchbar" action="/search" method="get")
            input(name="s" type="text" placeholder="Suchen nach...")
            button(type="navbarSubmit"): img(alt="Such-Icon" style="height: 20px; filter: saturate(0%) brightness(50%)" src="/public/icons/search_glass.png")

    //Login
    if !session.loggedin
        a(href="/login" style="float: right; color: rebeccapurple; background-color: lavender; padding: 5px;" +
            "font-weight: bold; border-radius: 5px; margin: 10px") Login
    else
        a(href="/login" style="float: right; color: darkred; background-color: mistyrose; padding: 5px;" +
            "font-weight: bold; border-radius: 5px; margin: 9px") Logout
        - session.loggedin = false
        - session.loggedout = true
        a(href="/list/" + session.username style="text-align: center; float: right; color: white; padding: 5px;" +
            "background-color: rebeccapurple; font-weight: bold; border-radius: 5px;" +
            "margin: 9px")= session.username + "s Liste"